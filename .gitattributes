# Normalize EOL for all files that Git considers text files.
* text=auto eol=lf
*.zip filter=lfs diff=lfs merge=lfs -text
*.mov filter=lfs diff=lfs merge=lfs -text
*.mov.zip filter=lfs diff=lfs merge=lfs -text
